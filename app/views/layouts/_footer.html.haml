%footer.footer
  .container-fluid
    .btn-group.pull-right{:style=>'margin-top:5px; '}
      .btn
        =link_to 'Home', root_path
      .btn
        =link_to 'About', static_pages_about_path
      .btn
        =link_to 'MailGun', new_mailgun_path
      -if current_user
        .btn
          =link_to 'My Tasks', tasks_path
        -if current_user.is_admin
          .btn
            =link_to 'Users', users_path
    %p.navbar-text
      Everyone deserves to become her best self